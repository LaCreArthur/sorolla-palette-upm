# This copy ships in the SDK package QA~/ and is version-pinned with the SDK: it is
# the canonical signal-marker list for this SDK version.

# Canonical signal markers for Palette SDK games.
# One substring per line. Lines starting with # are ignored.
# This is the grep list applied to a RAW capture:
#   iOS:     a devicectl --console rolling log or an Xcode Console export
#   Android: an adb logcat rolling log
# Usage:
#   grep -Ff <(grep -v '^#' signal-markers.txt | grep -v '^$') <capture>
# (strip comments/blanks first: grep -Ff treats comment lines as patterns too)

# Palette SDK
[Palette
[Palette:
[Consent Diagnostics]

# Firebase
[Firebase
FirebaseRemoteConfig
FirebaseCrashlytics

# Consent Mode v2 (cross-check ATT <-> ad_personalization).
# Mostly already caught by the [Palette / [Firebase prefixes; listed explicitly so the
# consent cross-check survives any future prefix narrowing. Do NOT add att_decision /
# consent_resolved / consent_changed — those are TrackEvent events, not Debug.Log lines,
# so they never appear in the device-log capture.
Consent mode signals:
Consent updated by MAX CMP:
MAX consent resolved:
ConsentStatus:
SetAdvertiserTrackingEnabled(
Firebase init failed — dropping analytics events
Event submission:

# GameAnalytics (both iOS NSLog and Android logcat tag forms)
Info/GA
Info/GameAnalytics: Add
PROGRESSION event
BUSINESS event
DESIGN event
RESOURCE event
ERROR event

# IAP wrappers
[IAP]
Purchase
StoreKit
BillingClient
TrackPurchase

# AppLovin MAX
[AppLovinSdk]
[AppLovin MAX]
[AppLovin MAX] Invoking event
[AppLovin MAX] Listener
OnInterstitial
OnRewarded
TrackAdRevenue

# Facebook
[FB]
Application has been deleted

# Adjust
[Adjust]

# Consent / privacy
UMP

# Crashes / exceptions
F AndroidRuntime
E AndroidRuntime
FATAL EXCEPTION
NullReferenceException
Exception
